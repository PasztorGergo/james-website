import React from "react";
import styles from "../styles";

const Navbar = () => {
  return (
    <nav className="w-full py-8 absolute top-0 left-0 px-16">
      <h2 className={`${styles.subTitle} uppercase`}>James.</h2>
    </nav>
  );
};

export default Navbar;
